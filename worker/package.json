{
  "name": "fcpm-broker",
  "private": true,
  "type": "module",
  "description": "The broker. Deliberately NOT at the repository root — a package.json up there changes how Cloudflare and wrangler detect the project, and the site itself is Ruby.",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
