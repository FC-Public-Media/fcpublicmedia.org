# A made-up class, to build and test class mode against (class-mode.md).
# A real class's folder has the same shape and can live in a private
# repository, mounted as a submodule: nothing here is special to the sample.
#
# One folder per kind of material, named by its noun (Handouts, Examples,
# Links...). Inside, one file per section, shown in name order, so numbering
# them keeps them in order. Markdown or plain text, shown as given.
title: Podcasting 101 (sample)
presenter: Sample Presenter
hours: 6:00–8:00 PM          # later, from the class's calendar entry
