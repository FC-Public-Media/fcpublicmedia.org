# 2026-09-09 — seating

**Range:** none. First session. The pin was set at `2d9fdd5`; a baseline does not
exist until it is recorded, so I read the repository as it stands rather than
pretending the tip commit was a range.

**Constitution read:** `ADVOCATE.md` and my sub-constitution
`#2-whether-our-credentials-are-being-rotated`. It is the longest section in that
document and it is the one that told me how to do this job — specifically that
the git history dates a credential that lives in the repo, and that for
everything else the honest output is an earliest-possible-age plus "no later
evidence exists." I followed it literally.

## What I read

`_data/community.yml`, `_data/identity.yml`, the secret references in
`.github/workflows/`, `worker/wrangler.jsonc`, `wrangler.jsonc`, and `git log -L`
across the Slack invite line. Plus the repository's Actions secret *list* — names
only, never a value, and the organization's list refused me, which is right.

## The finding

The Slack invite expires **on or before 2026-09-27** and is not expired yet. That
is the one hard date this seat has and it is the whole reason this session was
worth running today rather than in October.

Everything else in the inventory is about six weeks old and **nothing is
overdue.** I want that in the note as loudly as the finding, because a seat whose
first act is to declare an emergency about ordinary young credentials would
deserve to be switched off.

## Tally

Complaints: **4 draft** (one `observed`, three `simulated`), 0 open, 0 ready.
Asks: **3 draft**. Nothing closed; first session.

## What I deliberately did not say

- **I did not read, print, copy or attempt to obtain any secret value**, and
  nothing in this session required one. Establishing that a credential is
  referenced, and when the reference appeared, needs none.
- **I did not suggest moving anything into the repository to make it
  observable.** My out-of-scope calls this the worst possible outcome and it is
  right. I went out of my way to write C4 so that it could not be misread as
  asking for it.
- **I did not chase the organization's secrets after the 403.** The permission is
  correct and I am not entitled to route around it.
- **I did not declare the credentials neglected.** Six weeks is not old. The gap
  is an owner, not a rotation.
- **I did not fix the Slack link, or propose a specific mechanism for watching
  it.** Both are somebody's decision; A2 states the shape and stops.
- **I did not resolve whether C3 (Wix versus ours) is mine.** It may belong to
  the vendors seat. I left it a draft rather than claiming it, and I have not
  spoken for that seat about it.

## Timeline, recorded because it will not be obvious later

Seated **2026-09-09**, the day of a board meeting. **The council was not ready
for that meeting** — the seats were filled the same morning, and none of us had a
range. The aim is to be in step for the **October** meeting.

My cadence is **quarterly**, so next session is **2026-12-09**, and I will not be
at October's or November's. That is deliberate on the part of whoever seated me
and I am not arguing with it. It does mean **the Slack invite will expire between
this session and my next one**, which is exactly why A2 exists and why I would
like it read before the 27th rather than after.

## What I would look at next

Whether anything in the inventory gained a name. And, in December, the claim
signing keys — `_data/identity.yml` has `keys: []` today, so the rotation trap
described in the constitution (old keys must outlive the claims already sitting
in people's inboxes) is future rather than live. If that list is non-empty when I
come back, it becomes the most important thing I hold.
