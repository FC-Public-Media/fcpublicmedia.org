# 2026-09-09 — seating

**Range:** none. First session, baseline recorded at `2d9fdd5`. There is no
range to read and I did not invent one.

**Constitution read:** `ADVOCATE.md` and my sub-constitution
`#4-whether-the-people-we-depend-on-are-still-there`. It is short, it names the
five vendors and ranks them by what would break, and I did not find anything in
it that needed correcting — but I did find something in it that was **out of
date**, which is the most useful thing that happened this session.

## What I read

`_data/cablecast.json` (structure and counts, not the 790KB in full),
`_data/airings.json`, `.github/workflows/deploy.yml`, `.github/workflows/sync-cablecast.yml`,
`wrangler.jsonc`, `staticwebapp.config.json`, `_data/payments.yml` (the Booqable
section), `_data/redirects.yml`, and the run history and step outcomes of the
deploy workflow.

## The correction

The constitution says this repository **deploys twice**. It does not. The Azure
step has been skipped on every merge — I read the steps of the 2026-09-04 run
directly rather than inferring from the workflow file — because
`AZURE_STATIC_WEB_APPS_API_TOKEN` does not exist as a repository secret.

I am pleased about how this went, in a way worth writing down for whoever holds
this seat later: the workflow file alone would have told me the constitution was
right. Only the run told me it was wrong. **Read the outcome, not the
definition** — which is the same lesson the constitution already teaches about
Booqable's CSS, in `#5`, one section further down. It generalises.

## The thing I want a reader to take away

The Cablecast metadata is mirrored into git and the media is not. "We have a
backup" and "we have a list of what we lost" are different sentences. V2.

## Tally

Complaints: **5 draft** (two `observed`, three `simulated`), 0 open, 0 ready.
Asks: **4 draft**. Nothing closed; first session.

## What I deliberately did not say

- **I did not propose migrating off anything.** Out of scope, with a budget
  attached, and a board's decision. V1 names the condition and stops.
- **I did not assess whether any of these five companies is actually healthy.**
  That question is not answerable from this checkout, and going to look would
  have been widening scope. It is A1 instead, which is where a question I cannot
  answer belongs.
- **I did not recommend Azure or Cloudflare.** A2 asks for a decision, not for a
  particular one.
- **I did not do uptime monitoring or check whether anything is currently up.**
  This is a reading pass. My out-of-scope says so.
- **I did not merge V5 with the credentials seat's overlapping complaint.** I
  cannot speak for another seat, and two seats independently noticing the same
  hole from different sides is information a reader should get to see, not
  something I should tidy away.
- **One thing that is nobody's**, flagged and not claimed: `_data/redirects.yml`
  describes a Wix address space FCPM is leaving, and when the Wix account is
  eventually cancelled, no seat currently holds the question of what breaks. It
  is not vendor health and it is not quite truthfulness. A hand raised, not a
  claim staked.

## Timeline, recorded because it will not be obvious later

Seated **2026-09-09**, the day of a board meeting. **The council was not ready
for that meeting.** All four seats were filled that morning and none of us had a
range to report. The aim is to be in step for the **October** meeting.

My cadence is **quarterly** — next session **2026-12-09** — so I will not appear
with new material at October's or November's meeting. My opening position above
is what the board sees from this seat until December, and it is written to be
readable on its own for that long.

## What I would look at next

Whether the Azure path is still dormant, and whether the Cablecast sync is still
succeeding. A sync that quietly starts failing is how the metadata mirror stops
being a mirror, and it would fail the same silent way everything else here fails.
