# Git's per-user ignore file (~/.config/git/ignore, used when core.excludesFile
# is unset). Carried by ../../MANIFEST.
#
# .pr: the plan progression of the branch a worktree is on. Line 1 becomes the
# pull request's title and the rest its body; it is revised ahead as the work
# goes, and never committed (~/code/AGENTS.md, "Working in git").
.pr
