source "https://rubygems.org"

# The same floor `main` carries, for the same reason: `.ruby-version` is the
# only pin, and asdf does not read it without a per-user setting. Bundler
# refusing by name beats Jekyll failing obscurely.
ruby ">= 3.2"

gem "jekyll", "~> 4.3"
